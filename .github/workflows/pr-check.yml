name: pr-check

on:
  pull_request:
    branches:
      - main

jobs:
  pr_check:
    runs-on: ubuntu-latest
    steps:
      - name: PR Check Run
        run: echo "PR check running for main branch"




    
